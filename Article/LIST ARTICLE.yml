info:
  name: LIST ARTICLE
  type: http
  seq: 1

http:
  method: GET
  url: https://webapi.myindah-app.com/api/public/posts

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  # **GET POST**

  Menampilkan data artikel dengan nama **POST** secara keseluruhan menggunakan method **GET**
