meta {
  name: VIEW
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/mobileapps/users
  body: none
  auth: inherit
}

headers {
  Accept: application/json
}

settings {
  encodeUrl: true
}

docs {
  # **GET CURRENT USER**

  Returns the authenticated user from the JWT.
}
